@inject IJSRuntime _jsHelper;

@code {
    [Parameter]
    public string PageTitle { get; set; } = "Restaurant";

    protected override async Task OnInitializedAsync()
    {
      await _jsHelper.InvokeAsync<object>("setPageTitle", PageTitle);
    }

}